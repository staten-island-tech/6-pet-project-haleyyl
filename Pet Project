class Pet:
    def __init__(self,name, hunger = 63, happiness = 65, hygiene = 50):
        self.name = name
        self.hunger = hunger
        self.happiness = happiness
        self.hygiene = hygiene
    
    def play(self):
        play = input("would you like to play outside?")
        if play == "yes":
            print("the pet is now going outside")
            self.happiness += 20
            self.hygiene -= 20
            print(self.hygiene)

    def no (self):
        
        if self.hunger <= 25:
            print((f"{self.name} is very hungry"))
        if self.happiness <= 20:
            print((f"{self.name} is unhappy"))
        if self.hygiene <= 30:
            print((f"{self.name} is stinky"))
    def yay(self):
        if self.hunger >= 80:
            print((f"{self.name} is satisfied with his meal"))
        if self.happiness >= 80:
            print((f"{self.name} is very happy"))
    
    def statDec(self):
        self.hunger -= 5
        self.happiness -= 5
        self. cleanliness -= 5

    def showStatus(self):
        print(("{self.name} is {self.hygiene} % clean, {self.happiness} % happy, {self.hunger} % full"))

class Person:
    def __init__(self,my_name,pet):
        self.my_name = my_name

Jen = Pet("Pet")
Jen.play()


''' class Dog:
    def __init__(self,name,happiness):
        self.name = name
        self.__happiness = happiness
    def play(self,play):
        self.__happiness += play
    def show_status(self):
        print(f"{self.name} is now at {self.__happiness} % happiness")
Juno = Dog("Juno",5)
Juno.play(10)
Juno.show_status()
 '''