class Pet:
    def __init__(self,name, hunger = 60, happiness = 65, hygiene = 50, living = True):
        self.name = name
        self.hunger = hunger
        self.happiness = happiness
        self.hygiene = hygiene
        self.living = living

    def show_status(self):
        print(f"{self.name} has {self.hunger} hunger")
        print(f"{self.name} has {self.happiness} happiness")
        print(f"{self.name} has {self.hygiene} hygiene")

    def statDec(self):
        self.hunger -= 5
        self.happiness -= 5
        self. cleanliness -= 5
    

    
        



print("welcome to Make a Pet")
name = input("please enter a name for your pet")
stats = Pet((name),60,65,50,True)
pet = Pet(name)

while pet.living == True:
    input("Here are your stats. What would you like to do today?")

    

''' class Person:
    def __init__(self,my_name,pet):
        self.my_name = my_name

 '''


''' class Dog:
    def __init__(self,name,happiness):
        self.name = name
        self.__happiness = happiness
    def play(self,play):
        self.__happiness += play
    def show_status(self):
        print(f"{self.name} is now at {self.__happiness} % happiness")
Juno = Dog("Juno",5)
Juno.play(10)
Juno.show_status()
 '''

'''   def play(self):
        play = input("would you like to play outside?")
        if play == "yes":
            print(self.name "is now going outside")
            self.happiness += 20
            self.hunger -= 10
            self.hygiene -= 20
        if play == "no":
            self.happiness -= 10 '''

